use egui::Response;

use crate::types::Filament;

use super::App;

pub fn filament_picker_widget(ui: &mut egui::Ui, filament: &[Filament]) -> Response {
  let popup_id = ui.auto_id_with("popup");

  unimplemented!()
}
